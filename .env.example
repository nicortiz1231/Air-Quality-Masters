# Web3Forms access key for the service request form.
# Get one free at https://web3forms.com — it is tied to the destination inbox
# and is safe to expose in client-side code.
#
# Without this the form renders a "not connected yet" panel pointing at the
# phone number instead of silently discarding submissions.
VITE_WEB3FORMS_KEY=
